<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jb.boot.biz.work.mapper.WorkAssignMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, work_order_id, assign_count, updated_by, updated_time, created_by, created_time
    </sql>

    <select id="pageInfo"
            resultType="cn.jb.boot.biz.work.vo.response.WorkAssignPageResponse"
            parameterType="map">
        SELECT
        tws.id,
        two.work_order_no,
        two.device_id,
        two.dept_id,
        tws.created_time,
        tws.created_by,
        two.procedure_name,
        tws.assign_count,
        two.plan_total_count,
        two.item_no
        FROM t_work_assign tws
        INNER JOIN t_work_order   two ON two.id = tws.work_order_id
        LEFT JOIN  mes_item_stock mis ON mis.item_no = two.item_no
        <where>
            <if test="p.workOrderNo != null and p.workOrderNo != ''">
                AND two.work_order_no LIKE CONCAT('%', #{p.workOrderNo}, '%')
            </if>
            <if test="p.deviceId != null and p.deviceId != ''">
                AND two.device_id = #{p.deviceId}
            </if>
            <if test="p.deptId != null and p.deptId != ''">
                AND two.dept_id = #{p.deptId}
            </if>
            <if test="p.itemNo != null and p.itemNo != ''">
                AND two.item_no = #{p.itemNo}
            </if>
            <!-- 通过 mes_item_stock 的 bom_no 条件过滤 -->
            <if test="p.bomNo != null and p.bomNo != ''">
                AND mis.bom_no LIKE CONCAT('%', #{p.bomNo}, '%')
            </if>
            <if test="p.procedureNames != null and p.procedureNames.size() > 0">
                AND two.procedure_name IN
                <foreach collection="p.procedureNames"
                         item="name" open="(" close=")" separator=",">
                    #{name}
                </foreach>
            </if>
            <if test="p.beginTime != null and p.beginTime != ''">
                AND tws.created_time &gt; STR_TO_DATE(CONCAT(#{p.beginTime,jdbcType=VARCHAR}, ' 00:00:00'),
                '%Y-%m-%d %H:%i:%s')
            </if>
            <if test="p.endTime != null and p.endTime != ''">
                AND tws.created_time &lt;= STR_TO_DATE(CONCAT(#{p.endTime,jdbcType=VARCHAR}, ' 23:59:59'),
                '%Y-%m-%d %H:%i:%s')
            </if>
        </where>
        ORDER BY tws.created_time DESC
    </select>



</mapper>
